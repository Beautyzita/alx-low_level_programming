include "main.h"
/**
 * add - it add the sum of two integers
 * @a: the first int in the question
 * @b: the second int in the question
 * Return: this returns the sum of two integers
 */
int add(int a, int b)
{
	return (a + b);
}
