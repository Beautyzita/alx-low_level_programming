#include "main.h"

/**
 * 6-abs.c - this is to change positive number to nagitive
 * @n: the value in the question
 * Return: int _abs(int)
 */
int_abs(int n)
{
	if (n >= 0)
		return (n);
else
	return (-n);
}
