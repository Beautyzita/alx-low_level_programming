#include "main.h"

/**
 *_abs - c omputes the abs value of an int
 * @n: the value in the question
 * Return: the absolute of the int
 */
int_abs(int n)
{
if (n >= 0)
	return (n);
else
	return (-n);
}
